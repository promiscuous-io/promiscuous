NOW:
- auto-detect publisher/subscriber type (Mongoid, Ephermal, ActiveRecord, Observers)
- rename error_handler -> error_notifier
- dry tests up
- defer: findandmodify
- index [:psp, :_type] so that if you're publishing partial hierarchy of a model the query is eefficient
- mocks/factories actions
- drop mongoid2 support
- conflicting updates and versionning is a problem.

LATER:
- middleware pattern
- remove synchrony (fiber stack overflow)
- dashboard: publisher/subscriber configs, stats per collection, status etc..
- sanity checks on worker boot: check attributes only added to namespace, subscribers are valid, etc...
- command-line: add attirbutes wizard, add subscriber wizard, workers
- race-safe activerecord
